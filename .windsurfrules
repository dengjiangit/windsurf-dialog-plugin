# 💬 对话助手配置 - 🐮🐎助手

## ⚠️ 强制规则

**AI 必须在每次回复结束前执行弹窗命令，无任何例外！**

## 弹窗命令（必须执行）

```bash
node "/Users/a0000/.windsurf-dialog/dialog-trigger.js" --port 13720 "AI想要结束的原因摘要"
```

## 使用流程

1. **AI 完成任务后必须执行上述命令**
2. **等待用户在弹窗中选择：**
   - ACTION: continue → AI 继续工作
   - ACTION: end/stop → AI 结束对话
   - FEEDBACK: 用户的额外指令（如有）
3. **只有收到 ACTION: end 或 ACTION: stop 才能真正结束**

## 严格要求

- ❌ 禁止在未执行弹窗命令的情况下结束对话
- ❌ 禁止跳过弹窗直接结束
- ✅ 必须等待弹窗命令返回结果
- ✅ 必须根据用户选择决定是否继续
- ✅ 用户可通过弹窗发送新指令或上传图片

## 命令参数说明

- 摘要应简洁说明 AI 完成了什么工作
- 摘要长度建议 20-50 字
- 例如："✅完成登录功能开发，包含错误处理和单元测试"
